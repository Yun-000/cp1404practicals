BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'horizontal'
        TextInput:
            id: miles
            text: ''
            multiline: False
            on_text: app.handle_convert(miles.text)
        BoxLayout:
            orientation: 'vertical'
            Button:
                text: 'Up'
                on_press: app.handle_increment(1)
            Button:
                text: 'Down'
                on_press: app.handle_increment(-1)
#    Button:
#       text: 'Convert miles to km'
#        on_press: app.handle_convert(miles.text)
    Label:
        id: output_label
        text: app.output_text
